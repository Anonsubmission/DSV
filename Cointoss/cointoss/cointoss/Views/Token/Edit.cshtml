@model cointoss.Models.Token

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Token</legend>

        @Html.HiddenFor(model => model.ID)

        <div class="editor-label">
            @Html.LabelFor(model => model.OAuthToken)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.OAuthToken)
            @Html.ValidationMessageFor(model => model.OAuthToken)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.EffectiveResult)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EffectiveResult)
            @Html.ValidationMessageFor(model => model.EffectiveResult)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
